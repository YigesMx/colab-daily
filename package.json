{
  "name": "colab-daily-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs && npm run validate:dist",
    "validate:dist": "node scripts/validate-built-site.mjs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.40"
  }
}
